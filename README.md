# US Power Grid Dashboard

A ResearchOS dashboard for US power / data-center transmission-risk mapping.

## Run

```bash
npm test
npm run check
npm start
```

Endpoints: `/api/health`, `/api/state`, `/api/export/markdown`.
